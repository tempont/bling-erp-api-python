# Bling ERP API SDK

Python SDK for the Bling ERP API v3.

This project keeps authentication delegated to `bling-jwt-auth`, exposes a
hand-written resource API, and reserves `src/bling_erp_api/models/generated/`
for OpenAPI-assisted models.
